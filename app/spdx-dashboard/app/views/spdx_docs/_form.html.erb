<%= form_for(@spdx_doc) do |f| %>
  <% if @spdx_doc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spdx_doc.errors.count, "error") %> prohibited this spdx_doc from being saved:</h2>

      <ul>
      <% @spdx_doc.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :spec_version %><br />
    <%= f.text_field :spec_version %>
  </div>
  <div class="field">
    <%= f.label :comment_id %><br />
    <%= f.text_field :comment_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
